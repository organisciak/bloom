If implementing a feature, check off (if in progress, check [-]). If partial progress, add subtasks.

[ ] Expand the features for backslash commands, where if somebody presses backslash while typing, it opens a popup with all the possible backslash commands. Currently, /ai and /claude, but there will be more.

[ ] Add a checkbox to Generation with the AI sidebar that asks the LLM to start all games at zero. 

[ ] Add a checkbox to generation settings that enforces that numbers in the gains function use the `slider(value, min, max, step`) functionality. 

[ ] Add a suggestions backslash command which teaches you - it suggests a tweak and explains why.

[ ] 